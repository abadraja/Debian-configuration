Source : http://milq.github.io/things-to-do-just-after-installing-ubuntu-debian/

sudo apt-get -y update;
sudo apt-get -y upgrade;
sudo apt-get -y dist-upgrade;
sudo apt-get -y autoremove;
sudo apt-get install -y build-essential cmake;
sudo apt-get install -y p7zip p7zip-full unrar-free unzip;
sudo apt-get install -y screen;
sudo apt-get install -y nano vim;
sudo apt-get install -y git subversion;
sudo apt-get install -y gdebi;
sudo apt-get install -y gparted pitivi;

Tools:
  Sublime Text: 	
  wget http://c758482.r82.cf2.rackcdn.com/sublime-text_build-3083_amd64.deb;
  sudo dpkg -i sublime-text_build-3083_amd64.deb
  
  Virtual Box:
  https://www.linuxbabe.com/debian/install-virtualbox-debian-9-stretch
  sudo apt-get install dkms;


Theme:
  sudo apt-get install -y murrine-themes;
  sudo apt-get install -y docky;
  su -;
mkdir ~/numix;
cd ~/numix;
git clone https://github.com/cldx/numix-gtk-theme ;
git clone https://github.com/numixproject/numix-icon-theme ;
git clone https://github.com/numixproject/numix-icon-theme-circle ;
cd ~;
mkdir .icons .themes;
cp -R numix/numix-gtk-theme .themes/ ;
cp -R numix/numix-icon-theme/Numix .icons/ ;
cp -R numix/numix-icon-theme-circle/Numix-Circle .icons/ ;
=> Set in tweak tool : GTK + : Adwaiata-dark  :: Icons : Numix-circle

ZSH part :
sudo apt-get install zsh;
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)";






